normalize compiler flags centrally while you’re doing this instead of letting every
leaf Makefile have its own 2011-vintage collection.

Tests — but I’d split this into two stages.  After the networking work, add proper unit/integration tests, especially loopback
connection, disconnect/reconnect, timeout, shutdown, failover policy parsing, properties parsing, and protocol round trips.
I agree with not resurrecting the entire old Boost.Test graveyard; mine it for useful cases.

Protocol/serialization modernization. Yes, and I’d intentionally
defer this until the existing protocol has tests. The XML-generated protocol classes are exactly the sort of thing where
you want golden-message/round-trip tests before changing representation.

CI, sanitizers/static analysis, and versioning.

Once it builds cleanly, get a GitHub Actions build running GCC 14 with -Werror, then add ASan/UBSan builds. And at some point
replace the removed SVN Revision/DriverName mechanism with something very simple based on a release version plus optional Git commit ID.

Roadmap:

build/install cleanup* → reproducible clean build* → warning cleanup/Werror* → baseline tests → Asio/cxxsockets surgery* →
expanded tests → formatting → protocol/serialization work → CI/sanitizers/release packaging.

Tests:
App start/stop/status tests.
Does agent die if an app dies?
Does server get notifications?

1.0:
Extend the copyright-header pass from .cc/.h to Makefiles and scripts, using the same baseline/history logic so untouched IBM files remain untouched.
Do a clean-checkout rebuild and install from scratch.
Test the installed tree, not just the build tree: binaries, libraries, config failure behavior, certificate-generation path, man pages/docs.
Move the install tree somewhere else and confirm the $ORIGIN RUNPATH work still holds.
Run the Docker integration/failover/restart tests one more time against the final code.
Do one final grep for checkout-path leaks such as /home/will/code/dpm, /home/will/code2/dpm, and /bgsys.
Tag 1.0 once that passes.

2.0:
Address the XML/wire-protocol legacy rather than merely modernizing the implementation around it.
Reconsider/replace the existing serialization/protocol pieces and clean up whatever protocol-era baggage remains.
Because the client API abstracts the protocol, try to contain those changes below that API rather than forcing callers to know about the wire format.
Packaging

3.0:
A graphical operator/administration console built on the existing client API
showing included connected agents, running binaries, placement, start/stop/restart controls, failures/history, failover events, and potentially log access.


1.0 install/release
Source release
    dpm-v0.0.3.tar.gz

Optional convenience binary
    dpm-v0.0.3-ubuntu24.04-x86_64.tar.gz

Reference build environment
    Dockerfile / build container

Installation
    make
    make install
    PREFIX=/somewhere make install
    DESTDIR=/staging make install
